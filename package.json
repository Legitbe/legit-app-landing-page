{
  "name": "legit-landing",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "node build.mjs",
    "dev": "node build.mjs"
  },
  "devDependencies": {
    "esbuild": "^0.28.0"
  }
}
